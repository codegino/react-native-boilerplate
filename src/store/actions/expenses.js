import { FETCH_DAILY_RECORD, FETCH_MONTHLY_RECORD } from './actionTypes';

export const fetchDailyRecord = () => ({
  type: FETCH_DAILY_RECORD,
});

export const authSignupSucceed = () => ({
  type: FETCH_MONTHLY_RECORD,
});
